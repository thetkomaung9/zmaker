import 'package:get_it/get_it.dart';

final sl = GetIt.instance;

// Register dependencies here. Keeping minimal so the app runs without codegen.
Future<void> setupLocator() async {
  // Example:
  // sl.registerSingleton<SomeService>(SomeService());
}
