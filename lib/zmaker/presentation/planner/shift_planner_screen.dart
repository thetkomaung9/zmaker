import 'package:flutter/material.dart';

class ShiftPlannerScreen extends StatelessWidget {
  const ShiftPlannerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('교대 근무 플래너')),
      body: const Center(child: Text('Shift planner 기능 추가 예정')),
    );
  }
}
