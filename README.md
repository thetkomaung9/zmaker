# ZMaker Pro (Android)

Features:

- Analog Clock + Calendar
- Auto-suggest Bedtime
- Alarm notifications (wake & bedtime)
- Sleep data saved to local DB (sqflite)
- Weekly sleep chart (fl_chart)

Run on Android:

```bash
flutter pub get
flutter run
```
